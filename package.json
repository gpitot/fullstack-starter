{
  "private": true,
  "scripts": {
    "build": "yarn lint && turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "start": "turbo run start --filter server"
  },
  "devDependencies": {
    "@turbo/gen": "^1.9.7",
    "eslint-config-base": "*",
    "eslint-config-web": "*"
  },
  "name": "fullstack-starter",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "dependencies": {
    "eslint": "^8.49.0",
    "turbo": "^1.10.13"
  }
}
