{
  "name": "interfaces",
  "version": "1.0.0",
  "description": "",
  "main": "generated/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "cd schemas/api && openapi -c fetch --output ../../generated -i api.yml --useOptions --useUnionTypes",
    "build": "npm run compile"
  },
  "author": "Guillaume Pitot",
  "license": "ISC",
  "devDependencies": {
    "openapi-typescript-codegen": "^0.23.0",
    "typescript": "^4.8.3"
  }
}
